{
  "name": "nestjs-scheduler-dashboard",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @nestjs-scheduler-dash/scheduler-dash build",
    "dev:ui": "pnpm --filter @nestjs-scheduler-dash/scheduler-dash-ui dev",
    "dev": "pnpm --filter @nestjs-scheduler-dash/scheduler-dash dev",
    "sample": "pnpm --filter sample dev",
    "sample:build": "pnpm build && pnpm --filter sample build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
