name: Publish to npm

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify tag matches package version
        run: |
          TAG="${GITHUB_REF_NAME}"
          PKG_VERSION="v$(node -p "require('./packages/scheduler-dash/package.json').version")"
          if [ "$TAG" != "$PKG_VERSION" ]; then
            echo "Tag ($TAG) does not match package version ($PKG_VERSION)"
            exit 1
          fi

      - name: Build
        run: pnpm build

      - name: Publish
        run: pnpm --filter @nestjs-scheduler-dash/dashboard publish --no-git-checks --access public --provenance
